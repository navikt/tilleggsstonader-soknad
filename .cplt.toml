# Generated by `cplt init` — review before trusting.
# Detected: Node.js, Docker, Playwright
# Workspace members:
#   src/backend (heuristic scan) — Node.js

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: (root)
allow_docker = true

# The following are machine-specific — add to ~/.config/cplt/config.toml:
# sandbox.allow_cache_exec += ["ms-playwright"]  # in personal config

