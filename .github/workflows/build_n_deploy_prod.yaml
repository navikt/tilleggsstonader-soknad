name: Bygg og deploy til dev og prod

on:
  push:
    branches:
      - 'main'
    paths:
      - 'src/**'
      - '.nais/prod.yaml'
      - '.nais/dev.yaml'
      - 'webpack/**'
      - '.dockerignore'
      - '.npmrc'
      - '.nvmrc'
      - 'Dockerfile'
      - 'package.json'
      - 'tsconfig.json'
      - 'yarn.lock'
      - 'pnpm-lock.yaml'

jobs:
  playwright:
    name: Tester app med Playwright
    permissions:
      contents: read
    uses: navikt/tilleggsstonader/.github/workflows/run-playwright-pnpm.yml@pnpm-workflows
    secrets: inherit
    with:
      node-version: "24"
  build_and_deploy:
    name: "Bygger app og deployer til prod og dev"
    needs: playwright
    uses: navikt/tilleggsstonader/.github/workflows/ts-build-and-deploy-prod-and-dev-pnpm.yml@pnpm-workflows
    secrets: inherit
    permissions:
      id-token: write  # Trengs for deploy til GCP
    with:
      prod-cluster: 'prod-gcp'
      prod-manifest: '.nais/prod.yaml'
      dev-cluster: 'dev-gcp'
      dev-manifest: '.nais/dev.yaml'
      node-version: "24"